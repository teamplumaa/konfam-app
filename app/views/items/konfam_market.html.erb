<section>
  <div class="row">
    <% @items.each do |item| %>
      <div class="box">
        <div class="boxInner">
          <%=link_to item_path(item) do %>
            <%= image_tag item.cover_photo(:original) %>
          <% end %>
        </div>
      </div>
  <% end %>
  </div>
</section>
